name: Second CI

on:
    # push
    workflow_dispatch:

jobs:
    build:
        runs-on:  ubuntu-latest

        steps:
            - name: Say hello
              run: echo "hello from second workflow"